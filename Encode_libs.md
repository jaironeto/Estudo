# CSS example | code study

[NOT] `Slideshow`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3ASlideshow+tag%3Acss#
[NOT] `Form`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3AForm#
[NOT] `Accordion`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3AAccordion#
[NOT] `Dropdowns`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3ADropdowns#
[NOT] `Barra`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3Abarra#
[NOT] `Menu`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3Amenu#
[NOT] `Slideshow`
[NOT] `Button`https://hypothes.is/groups/99gVerPe/estudo?q=tag%3AButton#`

parei: https://www.w3schools.com/howto/howto_css_alert_buttons.asp
# lib last view
# Javascript Photography Snipshoot


# C# 

- array simples
- acessar membro
- args
- classe
- construtor primario
- nivel superior
- interface
- delegate nivel superior
- delegate dentro de uma classe
- enum
- namespace
- struct
- using
- main
- metodo
- return
- tipo object
- tipo
- objeto e instanciação
- required
- inicializador de objeto com e sem ()
- herança
- record posicionais, criar e acessa
- record sem parametros posicionais, criar e acessar
- with
- tipo anulavel
- classe generica
- metodo generico com tipo de retorno normal e outro generico
- instanciação de classe genérica com parâmetros de tipo com mais de 1 argumento
- instanciação de metodo genérica com parâmetros de tipo
- tipo anonimo 
- array de tipos anônimos
- polimorfismo (override, new, virtual)
- foreach
- string
- classe abstract
- metodo sobrecarregado 
- metodo abstract
- metodo static
- conversão
- argumento nomeado
- if..else
- passando parametro
- params
- tupla criada com var
- tupla criada com Tuple tipo
- tupla criado com declaração de tupla com tipos específicos sem inferencia de nome
- tupla atribuição de nomes a campos de tuplas com declaração explícita de tipos e inferência de nomes
- async
- passar parametro por referencia
- retornar por referencia
- parametro sendo uma matriz com uma ou mais dimensão
- parametro opcional
- membros aptos para expressão
- inicializar propriedade auto implementada
- propriedade normais
- propriedade auto-implementada
- propriedade com membro apto para expressçao
- propriedade com validação, throw
- exemplo palavra chave value em propriedade
- indexador
- indexador com 2 argumentos para campos de array com +1 dimensão
- yield return
- multiplos yield return
- iterator async
- consumir um iterator com foreach ou while
- await

# Express
- fazer downcast para acessar uma interface a partir de uma classe herdada

exemplo simples express.js hello world
responda à solicitação POST na rota raiz ( /), a página inicial do aplicativo
responda a uma solicitação PUT para a /userrota
responda a uma solicitação DELETE para a /userrota
Por exemplo, use a static função para exibir imagens, arquivos CSS e arquivos JavaScript em um diretório denominado public
Usar vários diretórios de recursos estáticos, chame a express.static função de middleware várias vezes
Criar um prefixo de caminho virtual (onde o caminho não existe de fato no sistema de arquivos) para arquivos servidos pela express.static função, especificando um caminho de montagem para o diretório estático
Este exemplo mostra uma função de middleware sem caminho de montagem. A função é executada toda vez que o app recebe uma requisição.
Neste exemplo, a rota /mypath é manipulada pelo router.get, e em seguida, o roteador é montado como um middleware no aplicativo principal usando app.use(router).
Este exemplo mostra uma matriz com uma subpilha de middleware que manipula solicitações GET para o /user/:idcaminho
app.use uma função de middleware sem caminho de montagem. Este código é executado para cada solicitação ao roteador. app.use a middleware sub-stack shows request info for any type of HTTP request to the /user/:id path. uma subpilha de middleware que lida com solicitações GET para o caminho /user/:id. manipulador para o caminho /user/:id, que renderiza uma página especial. monte o roteador no app
O exemplo a seguir ilustra a instalação e o carregamento da função de middleware de análise de cookie cookie-parser.
três funções de middleware que são executadas em sequência para a rota /my-route. Cada uma delas é responsável por chamar o próximo middleware usando next(), e o último middleware envia a resposta para o cliente usando res.send()

# Typescript

- adicionando novos campos a uma interface existente com interface e com type
- alias de tipo para nomear um tipo de função
- alias para um tipo união e depois usar o alias na função
- aliases de tipo para tipos, objeto e função
- anotação Tipos de objeto em funções
- anotações de Tipo de Retorno
- anotações de tipo em variáveis
- as duas formas de declarar matrizes
- asserções de tipo com as do menos especifico para o mais especifico
- asserções de tipo com as e com <>
- assinatura de construção
- assinatura de indice em classes
- assinatura de índice 
- campos de classe prefixados com readonly com e sem modificador public
- classe blocos estaticos
- classe com membro private
- classe com membro protected
- classe com membro public
- classe e membros abstract
- classe função com uma assinatura de construção, no caso, implements em uma interface com uma
- classe membros static 
- classe membros static com modificadores de acesso
- classe membros static herdado
- classes genericas
- combinação de uma função com uma propriedade associada em um tipo, que pode ser usado como parâmetro em uma função normal
- compilar o codigo
- construtor com campos publicos diretamente inserido
- declarar uma classe com campos, construtor e metodos
- declarar uma função que pode ser chamada com ou sem new, no caso, uma função normal e outra construtora em uma interface Implementado por uma classe e um objeto literal
- definindo um tipo de união em uma função
- desestruturar tuplas, const com tipo tupla
- desestruturação de parametros de array em uma função, sendo 3 exemplos de tupla, array string, uniao string e undefined
- desestruturação de parâmetros em uma função
- enums
- especificar o argumento passado para a função génerica
- estendendo e tambme implementando uma interface
- estendendo um tipo por meio de interseções
- estreitamento em tipos de união
- exemplo TypeScript declare o tipo this no corpo da função
- extends em classes
- fornecer parametro padrão para uma função
- função Genérica
- funções Genéricas com restrições
- funções Genéricas com varios parameotros
- get/set em classes
- implements em classes
- inferencia explicita ao chamar uma função génerica
- interface
- interface com um tipo generico
- membros normais e uma assinatura de indice para caso eu precise mais propriedades extra
- metodos em classes com tipagem em todos os tipos diferentes de membros possiveis
- operador de Asserção Não Nulo (Postfix !)
- overloads em construtores de classe
- parametro de funções opcionais com ? e valor padrão
- parametro rest em função
- parâmetros opcionais em retornos de chamada/calback
- primitiva symbol, bigint
- propriedade declarada diretamente no construtor da classe
- propriedade opcional
- propriedades Opcionais em funções
- propriedades marcadas como readonly
- readonly abreviado
- readonlyArray
- reandoly direto no parametro da função
- sobrecarga de função
- sobrescrever um método em uma classe
- this como parametro
- tipo any
- tipo de tupla 
- tipo void, object, unknown, never, Function
- tipos de tupla as const 
- tipos de tupla com elementos opcionais
- tipos de tupla com elementos rest
- tipos de tupla readonly
- tipos de tuplas usados em parâmetros e argumentos rest 
- tipos explícitos parametro de function
- tipos literais
- tipos literais em objeto, mas as duas formas usar const para manter imutavel
- trabalhar com tipos de união em uma função
- type com tipo generico
- type em tipos de objetos